{
  "private": true,
  "name": "jsonrpc-ws-proxy",
  "version": "0.0.1",
  "dependencies": {
    "js-yaml": "^3.12.0"
  },
  "devDependencies": {
    "@types/node": "^10.12.3",
    "@types/ws": "^6.0.1",
    "minimist": "^1.2.0",
    "rimraf": "^2.6.2",
    "typescript": "^3.0.1",
    "vscode-ws-jsonrpc": "file:../vscode-ws-jsonrpc",
    "ws": "^6.1.0"
  },
  "scripts": {
    "prepare": "npm run clean && npm run build",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "build": "tsc"
  }
}
